<div container="class">
  <h1>This are my Lists</h1>

  <% @lists.each do |list| %>
    <%= link_to list.name, list_path(list) %>
  <% end %>
</div>

<%= link_to "Add a List", new_list_path(@list) %>

<%= console %>

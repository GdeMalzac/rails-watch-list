<div class=container>
  <%= simple_form_for([@list, @bookmark]) do |f| %>
    <%# un truc à faire ici %>
    <%= f.association :movie %>
    <%= f.input :comment %>

    <%= f.submit %>
  <% end %>
</div>
